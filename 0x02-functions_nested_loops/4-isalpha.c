#include "main.h"
/**
* _isalpha - checks if lower case or not
* @c: is a character
* Return: 0 for uppercase or 1 for lowercase.
*/
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' &&  c <= 'Z'))
	{
	return (1);
	}
		else
		{
	return (0);
		}
/*bN*/
}
