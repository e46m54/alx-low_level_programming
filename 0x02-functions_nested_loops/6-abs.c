#include "main.h"
/**
* _abs - calculate absulute of int
* @n: input an integer
* Return: Absolute value of int
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
		else
	{
		return (n);
	}
/*bN*/
}
